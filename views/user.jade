extends layout
block content

	h1 Hello #{userData.name}

	- if(userData.facebook)
		p You logged in via Facebook.

	- if(userData.twitter)
		p You logged in via Twitter.

	h3 User Data in DB
	p= JSON.stringify(userData)

	h3 Session User Id
	p= JSON.stringify(userId)
	p This variable was set from our custom middleware to res.locals.userId and thus becomes available in this view.
